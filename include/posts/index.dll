<?php
session_start();

if(isset($_POST["cadastrar"])) { // Verifico se o botão cadastrar foi acionado
	
	if(!empty($_POST["assunto"]) && !empty($_POST["msg"])) { // Verifico se os campos foram preenchidos
		
		$msg = $class->antisql($_POST["msg"]); // Filtro os dados de login name originados do formulário
		$assunto = $class->antisql($_POST["assunto"]);		
				
		$insert = mysql_query("INSERT INTO post VALUES(null, '$id_gen', '$login', '$msg', '$assunto')") or die(mysql_error()); // Insiro os dados no BD
			
		if($insert) { // Verifico se a query foi executada com sucesso. Se sim, define mensagem de sucesso.
				
		$mensagem_erro = "<b>Mensagem postada com sucesso!</b>";
			
		}
		
	}
	else { // Se houver algum campo em branco, define mensagem de erro
	
		$mensagem_erro = "<b>Preencha todos os campos!</b>";
		
	}		
}
?>

<?php include("includes/ct.dll"); ?><span class="titulo">Criar Post:</span><?php include("includes/ft.dll"); ?>
<?php include("includes/cb.dll"); ?>

<form method="post" action="<?php $PHP_SELF; ?>">
<table width="600" border="0" cellspacing="0" cellpadding="5">
  <tr>
    <td><?php echo $mensagem_erro; ?></td>
  </tr>
  <tr>
    <td><table width="550" border="0" cellspacing="0" cellpadding="5"></td></tr>
      <tr>
        <td>Assunto:</td>
        <td><label>
          <input id="cordoinput" type="text" name="assunto" value="" />
		</label></td>
      </tr>
      <tr>
        <td>Mensagem:</td>
        <td><label>
          <textarea id="cordoinput" type="text" name="msg" rows="10" cols="100"></textarea>
		</label></td>
      </tr>
      <tr>
        <td>&nbsp;</td>
        <td><label>
          <input id="cordoinput" type="submit" name="cadastrar" value="OK" />
        </label></td>
      </tr>
    </table></td>
  </tr>
</table>
</form>
<a href="index.php?pag=perfil" target="_top">Voltar</a>
<?php include("includes/fb.dll"); ?></div>